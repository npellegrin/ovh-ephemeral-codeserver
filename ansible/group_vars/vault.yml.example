---
# Copy to group_vars/vault.yml, fill in real values, then encrypt it:
#   ansible-vault encrypt group_vars/vault.yml
# group_vars/vault.yml itself is gitignored and must never be committed
# unencrypted (see AGENTS.md: "Never write secrets in plaintext YAML").
# Secrets only. Non-secret config goes in vars.yml.

# --- OVH Object Storage backup user (set by `make generate-vault-vars`) ---
swift_user: "CHANGEME"
swift_key: "CHANGEME"

# --- code-server (the login password; set by `make generate-vault-vars`) ---
code_server_password: "CHANGEME"
